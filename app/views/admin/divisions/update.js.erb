<%if @division%>
var id = "#<%=@division.id%>"
$(id).remove('');
$("#division-listing").prepend("<%= j render partial: 'listing', locals: {:division => @division,:index => (100..500).to_a.sample} %>");
<%end%>
$('#add-division').find('.close').click();