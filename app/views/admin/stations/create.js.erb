<%if @station%>
$("#station-listing").prepend("<%= j render partial: 'listing', locals: {:station => @station,:index => (100..500).to_a.sample} %>");
<%end%>
$('#add-station').find('.close').click();
