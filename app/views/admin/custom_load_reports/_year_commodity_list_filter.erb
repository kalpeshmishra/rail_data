Commodity<p></p>
<%= select_tag "year-commodity-list", options_for_select(custom_year_commodity_list), {multiple: true, id: "year-commodity-list", class: 'primary-filters-second-page form-control input-sm'} %>


<script>
  $(function () {
    $('.primary-filters-second-page').multiselect({
      enableFiltering: true,
      enableCaseInsensitiveFiltering: true,
      includeSelectAllOption: true,
      buttonWidth: '100%',
      buttonClass: 'btn btn-default',
      numberDisplayed: 0,
      
    });

    $('#year-commodity-list').on("change",function(){
      $("#submit_error_msg").css('display','none');
    });

  });
</script>