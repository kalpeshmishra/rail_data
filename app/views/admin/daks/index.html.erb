<style>
  ul.dropdown-menu {
    width: 200px;
    max-height: 180px;
    overflow: auto;
  }
  html {
    overflow-x: scroll;
  }
  .noborder{
    border: hidden;
  }
</style>

<%if params[:dak_type] == "received" && params[:dak_type].present?%>
  <%= render 'received_dak_form' %>
<%end%>
<%if params[:dak_type] == "dispatch" && params[:dak_type].present?%>
  <%= render 'dispatch_dak_form' %> 
<%end%>
<%if params[:dak_type] == "search" && params[:dak_type].present?%>
  <%= render 'search_dak_form' %> 
<%end%>