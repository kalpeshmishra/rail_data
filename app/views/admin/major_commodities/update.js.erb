<%if @major_commodity%>
var id = "#<%=@major_commodity.id%>"
$(id).remove('');
$("#major_commodity-listing").prepend("<%= j render partial: 'listing', locals: {:major_commodity => @major_commodity,:index => (50..500).to_a.sample} %>");
<%end%>

$('#add-major_commodity').find('.close').click();